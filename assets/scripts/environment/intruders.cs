using UnityEngine;
using System.Collections;

public class intruders : MonoBehaviour {
   

    // Use this for initialization
    public GameObject intruder;

    void Start () {
        //Instantiate(intruder, Vector3.zero, Quaternion.identity);

        //Instantiate(intruder, Vector3.zero, Quaternion.identity);


    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
